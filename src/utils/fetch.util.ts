import {HttpHeaders} from '@angular/common/http';

export const URL_GATEWAY = 'https://api.littleworker.fr/2.0/';

export const HTTP_OPTIONS = { headers: new HttpHeaders({ 'Content-Type': 'application/json' })};
